DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE
# Cseréld ki a fenti sort a Render PostgreSQL connection string-re
# Megtalálod: Render Dashboard > PostgreSQL > Connect > External Connection

JWT_ACCESS_SECRET=replace_with_strong_secret
JWT_REFRESH_SECRET=replace_with_strong_refresh_secret

PORT=3000

CORS_ORIGIN=https://csetem.vercel.app
